# Commit scopes

Approved `type(scope)` scopes for this repository. A scope may only be used
if it is listed here; introduce a new entry before the first commit that uses
it. Plain `type: message` commits need no scope.

<!-- Approved scopes (initially empty): -->

<!--
- none yet
-->
